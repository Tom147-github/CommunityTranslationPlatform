"use client";

import { ReactElement } from "react";

const FormButton = (text): ReactElement => {
    return (
        <button>
          {text}
        </button>
    );
};

export default FormButton;