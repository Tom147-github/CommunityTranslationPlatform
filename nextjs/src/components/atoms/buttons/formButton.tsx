"use client";

import { ReactElement } from "react";

const FormButton = (): ReactElement => {
    return (
        <button>
        
        </button>
    );
};

export default FormButton;